fi:
  activerecord:
    values:
      custom_attribute:
        attribute_type:
          set: monivalinta
          numeric: numeerinen
          alpha: teksti
      image_type:
        purpose:
          presentational: esittävä
          technical: tekninen
          document: dokumentti
      property:
        value_type:
          string: sanallinen
          numeric: numeerinen
