$.fn.extend
  initialize: ->

    $('.gallery', this).each ->
      $(this).magnificPopup
        delegate: '.image-link'
        type: 'image'
        gallery:
          enabled: true

    $('[data-toggle="tooltip"]', this).tooltip()
    $('[data-toggle="popover"]', this).popover
      html: true
      trigger: 'hover'

    $('.select2', this).select2
      width: 'resolve'
      dropdownAutoWidth: true

    $('.wysihtml5', this).each ->
      $(this).wysihtml5
        locale: 'en-terse'
        stylesheets: ["<%= asset_path('fonts.css') %>"]

jQuery ->

  window.wiselinks = new Wiselinks()

  $(document).initialize()

  $(document).on 'change', 'form.immediate', (e) ->
    $(e.currentTarget).trigger('submit.rails')

  $(document).on 'click', 'button.stateful', (e) ->
    $(e.currentTarget).prop('disabled', true)
