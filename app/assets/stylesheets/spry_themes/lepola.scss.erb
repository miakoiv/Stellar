/*
 * Theme specific styles
 */

$brand-primary: lighten(#1a3476, 8%);
$navbar-inverse-bg: #1a3476;
$navbar-inverse-color: lighten(#1a3476, 40%);
$navbar-inverse-link-color: lighten(#1a3476, 45%);

@import "bootstrap-generators.scss";
@import "bootstrap-font-awesome.scss";
@import "bootstrap-tweaks.scss";
@import "fonts.scss";
@import "print.scss";

.lepola {
  overflow-y: scroll;
  background-color: lighten(#fab20b, 46%);
  background-image: url(<%= asset_path('lepola-bg.svg') %>);
  background-position: bottom left;
  background-repeat: no-repeat;
  background-size: 1000px 1000px;

  #main {
    background: transparent;
  }
  #main-nav {
    @extend .navbar-inverse;
  }

  .container {
    padding-left: $grid-gutter-width;
    padding-right: $grid-gutter-width;
    width: auto;
  }

  ul.categories {
    @extend .nav, .nav-pills;
    z-index: $zindex-navbar;
    position: fixed;
    left: 0;
    right: 0;
    top: $navbar-height;
    padding: 5px $grid-gutter-width/2;
    font-size: $font-size-small;
    background: #fff;
    border-bottom: 1px solid #1a3476;

    li.category {
      a {
        border-radius: 18px;

        img { display: none; }
        p { margin: 0; }
      }
    }
  }

  ul.products {
    list-style: none;
    position: relative;
    padding: 0;

    li.product {
      @extend .img-thumbnail;
      position: absolute;
      width: 180px;
      padding: 10px;
      margin: 0 10px 10px 0;
      background: #fff;
      border-radius: $border-radius-base;

      h3 {
        margin: 0.5em 0 0;
        font-size: $font-size-base;
      }
      .description, .buy-now {
        display: none;
      }
      &:hover {
        @include box-shadow(0 0 8px $input-border-focus);
        a { text-decoration: none; }
      }
    }
  }

  .product-detail {
    @extend .row;

    .product-actions {
      @extend .col-xs-12;
      @extend .col-sm-6, .col-sm-push-6;
      @extend .col-md-5, .col-md-push-7;
      @extend .col-lg-4, .col-lg-push-8;
      @extend .text-right;

      .form-inline {
        float: left;
        max-width: 200px;
      }
      a { display: inline-block; }
    }

    .product-title {
      @extend .col-xs-12;
      @extend .col-sm-6, .col-sm-pull-6;
      @extend .col-md-7, .col-md-pull-5;
      @extend .col-lg-8, .col-lg-pull-4;
      margin-top: 0;
      margin-bottom: 20px;
    }

    .product-description { display: none; }

    .images {
      .nav { display: none; }

      .tab-content {
        .image:not(:first-child) {
          @extend .hidden-print;
        }
      }

      img {
          @extend .img-responsive, .img-thumbnail;
      }
    }

    .images.technical {
      @extend .col-sm-4;
      @extend .col-sm-push-8;
    }
    .images.presentational {
      @extend .col-sm-8;
      @extend .col-sm-pull-4;
    }
  }
}

.footer {
  background: #1a3476;
  color: lighten(#1a3476, 40%);
}
