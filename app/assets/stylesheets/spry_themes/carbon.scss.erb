/*
 * Theme specific styles
 */

$brand-primary: #3fb8b8;
$body-bg: #fff;
$footer-bg: $brand-primary;

$font-size-base: 16px;
$font-family-serif: 'Droid Serif', serif;

$navbar-inverse-color: #bbb;
$navbar-inverse-border: transparent;
$navbar-inverse-link-color: #eee;
$navbar-inverse-link-hover-color: #fff;
$navbar-inverse-link-active-color: #fff;
$navmenu-inverse-color: #bbb;
$navmenu-inverse-link-color: #eee;
$navmenu-inverse-link-active-color: #fff;

@import "bootstrap-font-awesome.scss";
@import "bootstrap-generators.scss";
@import "bootstrap-tweaks.scss";
@import "fonts.scss";
@import "print.scss";

@include bg-variant('.bg-primary', $brand-primary);

@font-face {
  font-family: 'Droid Serif';
  src: font-url('droidserif-webfont.eot');
  src: font-url('droidserif-webfont.eot?#iefix') format('embedded-opentype'),
  font-url('droidserif-webfont.woff') format('woff'),
  font-url('droidserif-webfont.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
}

@font-face {
  font-family: 'Droid Serif';
  src: font-url('droidserif-italic-webfont.eot');
  src: font-url('droidserif-italic-webfont.eot?#iefix') format('embedded-opentype'),
  font-url('droidserif-italic-webfont.woff') format('woff'),
  font-url('droidserif-italic-webfont.ttf') format('truetype');
  font-weight: normal;
  font-style: italic;
}

@font-face {
  font-family: 'Droid Serif';
  src: font-url('droidserif-bold-webfont.eot');
  src: font-url('droidserif-bold-webfont.eot?#iefix') format('embedded-opentype'),
  font-url('droidserif-bold-webfont.woff') format('woff'),
  font-url('droidserif-bold-webfont.ttf') format('truetype');
  font-weight: bold;
  font-style: normal;
}

@font-face {
  font-family: 'Droid Serif';
  src: font-url('droidserif-bolditalic-webfont.eot');
  src: font-url('droidserif-bolditalic-webfont.eot?#iefix') format('embedded-opentype'),
  font-url('droidserif-bolditalic-webfont.woff') format('woff'),
  font-url('droidserif-bolditalic-webfont.ttf') format('truetype');
  font-weight: bold;
  font-style: italic;
}

@font-face {
  font-family: 'Montserrat';
  src: font-url('montserrat-bold-webfont.eot');
  src: font-url('montserrat-bold-webfont.eot?#iefix') format('embedded-opentype'),
  font-url('montserrat-bold-webfont.woff') format('woff'),
  font-url('montserrat-bold-webfont.ttf') format('truetype');
  font-weight: 500;
  font-style: normal;
}


/*
 * Extra tall navbar
 */
#main-nav {
  @extend .navbar-inverse;
  background-image: url(asset_path('carbon.png'));
  background-attachment: fixed;
}
#main-nav .container {
  padding-top: 30px;    // padding the container only,
  padding-bottom: 30px; // not the contained elements
}
#main-nav .navbar-brand {
  padding-top: 0;
  padding-bottom: 0;

  img { max-height: 50px; }
}

/* No main nav in this theme */
#main-nav .navbar-nav { display: none; }

/* Push content below the navbar */
#content-wrap, #offcanvas .inner-wrap { padding-top: 110px; }

/* Keep the hamburger on desktop */
.offcanvas-toggle {
  @extend .btn-lg;
  display: block !important;
}

/* Inverse offcanvas */
#offcanvas { @extend .navmenu-inverse; }


/*
 * Less prominent mobile navbar
 */
@media (max-width: $screen-xs-max) {
  #content-wrap, #offcanvas .inner-wrap { padding-top: 70px; }

  #main-nav .container {
    padding-top: 10px;
    padding-bottom: 10px;
  }
  #main-nav .navbar-brand {
    padding-top: 10px;
    padding-bottom: 10px;

    img { max-height: 30px; }
  }
}


/*
 * Rounded buttons all over
 */
#static-wrap .btn,
#store-wrap .btn {
  @extend .btn-round;
}


/*
 * Static content wrapper
 */
#static-wrap > .container {
  padding-top: 20px;
  padding-bottom: 20px;
}

/* Content sections */
.section {
  position: relative;
  padding-top: 60px;
  padding-bottom: 60px;
}
.section-primary {
  background-color: $brand-primary;
  color: #fff;
}
.section h2 { margin-bottom: 40px; }


/*
 * Content elements
 */

/* Thumbnails with various aspect ratios */
.thumbnail {
  position: relative;
  padding: 0;
  border: 0;
  border-radius: 0;
}
.thumbnail.three-by-two {
  height: 0;
  padding-bottom: 66.7%;
}
.thumbnail.square {
  height: 0;
  padding-bottom: 100%;
}
/* Thumbnail content takes up all available space */
.thumbnail .thumbnail-content {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 5%;
}
.center-content {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Background image */
.bg-image {
  background-repeat: no-repeat;
  background-position: center center;
  background-size: cover;
}
.bg-image .text-shadow { text-shadow: 0 0 6px rgba(0,0,0,0.8); }

.gradient-overlay:before {
  content: '';
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  @include gradient-vertical(rgba(0,0,0,0.8), rgba(0,0,0,0), 0%, 50%);
}
/* Translucent container */
.bg-translucent {
  padding: 20px 40px 40px;
  background-color: rgba(255,255,255,0.8);
}


/*
 * Typography
 */
p, address {
  font-family: $font-family-serif;
}
.larger { font-size: 1.1em; }
.extra-bold {
  font-family: Montserrat, sans-serif;
  font-weight: 500;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}
.text-white { color: #fff; }
.text-shadow { text-shadow: 0 0 6px rgba(0,0,0,0.2); }
.box-shadow { box-shadow: 0 0 6px rgba(0,0,0,0.2); }

ul, ol { padding-left: 1em; }
ul { list-style-type: square; }

.drop-cap {
  float: left;
  margin-right: 15px;
  margin-bottom: -5px;
  font-size: 3em;
  font-weight: 500;
  line-height: 1;
  letter-spacing: -0.2em;
}

.correspondence-form .form-control {
  border-color: transparent !important;
  border-radius: 0;
}

#search-panel-wrap {}

#categories-wrap {
  background-color: #ccc;

  .categories {
    > ul {
      list-style: none;
      padding-left: 0;
      display: flex;
      flex-flow: row wrap;

      > li.category {
        @extend .col-xs-6, .col-sm-3;
        position: static;
        flex: 0 1 auto;
        padding: 0;

        &.active a::after {
          content: '';
          display: block;
          position: absolute;
          left: 0;
          right: 0;
          top: 0;
          bottom: 0;
          @include gradient-vertical(rgba(255,255,255,0.75), rgba(255,255,255,0), 0%, 50%);
        }

        > a:hover img {
          transform: translate(-50%, -50%) scale(1.2);
        }

        > a {
          position: relative;
          display: block;
          height: 0;
          padding-bottom: 50%;
          overflow: hidden;

          img {
            position: absolute;
            display: block;
            width: auto;
            height: 100%;
            left: 50%;
            top: 50%;
            transform: translate(-50%, -50%);
            transition: all 0.8s ease-in-out 0s;
          }
          p {
            position: absolute;
            left: 0;
            bottom: 0;
            width: 100%;
            margin: 0;
            padding: 5px;
            font-size: $font-size-small;
            font-weight: 500;
            background-color: rgba(0,0,0,0.666);
            color: #fff;
          }
        }
        .subcategories-wrap { display: none; }
      }
    }
  }
}

#products-wrap {}

#product-detail-wrap {}

.footer {
  color: #fff;
  font-size: 0.8em;
  font-weight: 500;
}
.footer .navmenu {
  @extend .navmenu-inverse;
  background: transparent;
}
