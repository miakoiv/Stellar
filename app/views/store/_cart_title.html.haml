%h1.hidden-print
  - if @order.quotation?
    = icon('inbox', t('store.cart.order_or_quote'))
    %span.small= bootstrap_label(@order.billing_group)
  - else
    = icon('shopping-cart', t('store.cart.shopping_cart'))
%h1.visible-print-block= current_store
