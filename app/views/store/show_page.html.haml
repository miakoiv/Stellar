- meta_tags_for(@page)

#static-wrap
  .content{class: @page.albums.any? ? 'has-sidebar' : ''}
    %div{class: @page.wysiwyg? ? 'fr-view' : ''}
      = @page.content.html_safe if @page.content.present?

- if @page.albums.any?
  = render partial: 'albums/sidebar', object: @page.albums, as: :albums
