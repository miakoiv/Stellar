- meta_tags_for(@promotion)

#store-wrap
  #store-header
    #categories-wrap
      .categories.hidden-print
        %ul= render @categories

  #store-body
    = render partial: 'show_promotion'
    = render partial: 'stock_handler'
