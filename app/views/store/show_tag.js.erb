$('#product-view').html('<%= j render partial: 'products/grid_view', object: @products, as: :products, locals: {grid_type: current_store.masonry? ? 'staggered-grid' : 'straight-grid'} %>');
$('.products').masonry_build()
