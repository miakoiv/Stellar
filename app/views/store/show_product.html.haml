- set_meta_tags(og: {title: @product.to_s, url: request.original_url, image: @product.cover_image.present? ? image_url(@product.cover_image.url(:presentational)) : nil, description: strip_tags(@product.description)})

.categories.hidden-print
  %ul= render @categories
  = render partial: 'subcategories'

.search-panel{style: 'display: none'}

.products{style: 'display: none'}

.product-detail
  = render partial: 'products/product_detail', object: @product, as: :product

:coffee
  jQuery ->
    if $(window).isSmall()
      $(window).scrollTo $('.product-detail'), 300, offset: -70
