.page-header.visible-print
  %h1
    = current_store.to_s
    %small= @category.to_s
.product-grid.collapse.in
  %ul= render @products
.product-list.collapse
  .panel.panel-default
    = render partial: 'products/table', object: @products, as: :products

:coffee
  $('.subcategories').replaceWith("#{j render partial: 'subcategories'}")
  $('.categories > ul > .category').deactivate()
  $("##{dom_id(@category)}").activate()
  $('.search-panel').hide()
  $('.product-detail').hide()
  $('.categories').show()
  $('.products').show()
  $('.navbar-form').show()

  if $(window).isSmall()
    $(window).scrollTo $('.products'), 300, offset: -70
