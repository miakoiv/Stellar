- cache_if current_user.guest?, [I18n.locale, :offcanvas_item, item] do
  %li
    = link_to show_category_path(item) do
      = image_variant_tag item.cover_image(:technical), :icon
      = item
    %ul.nav.navmenu-nav
      = render partial: 'categories/offcanvas_item', collection: item.children.visible, as: :item
