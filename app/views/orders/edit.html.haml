.page-header
  = link_to orders_path(anchor: orders_tab(@order)), class: 'btn btn-default' do
    = icon('chevron-circle-left', t('actions.back'))
  %h1
    = title(Order)
    %span.small= @order.to_s

.row
  .col-sm-5
    .panel.panel-default
      .panel-body
        = render partial: 'form'

  .col-sm-7
    = render partial: 'order_items/table', object: @order, as: :order
