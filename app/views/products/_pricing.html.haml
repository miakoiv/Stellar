- unless product.sales_price.nil?
  %p.currency= icon('tag', humanized_money_with_symbol(product.user_price(current_user)))
  - unless product.user_unit_price(current_user).nil?
    .unit-price.currency
      = unit_price_string(product.user_unit_price(current_user), product.base_unit)
