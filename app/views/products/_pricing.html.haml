- unless product.retail_price.nil?
  %p.currency= icon('tag', humanized_money_with_symbol(product.retail_price))
  - unless product.unit_price.nil?
    .unit-price.currency
      = unit_price_string(product.unit_price, product.base_unit)
