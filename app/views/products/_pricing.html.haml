- unless product.retail_price.nil?
  %p.currency= icon('tag', humanized_money_with_symbol(product.price(current_user)))
  - unless product.unit_price.nil?
    .unit-price.currency
      = unit_price_string(product.unit_price(current_user), product.base_unit)
