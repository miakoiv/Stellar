%ol.breadcrumb
  - if @category.present?
    - @category.self_and_ancestors.live.each do |ancestor|
      %li[ancestor]<
        = link_to ancestor, show_category_path(ancestor), data: {push: 'partial', target: '#store-body'}
