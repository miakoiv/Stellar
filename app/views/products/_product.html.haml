= div_for product do
  = link_to show_product_path(product) do
    - if product.cover_image.present?
      = image_tag product.cover_image.attachment.url(:thumbnail),
          class: 'img-responsive'
    %h4= product.name
