.row
  .col-sm-2
    %ul.nav.nav-pills.nav-stacked{role: 'tablist'}
      - @categories.each_with_index do |category, index|
        - next if category.products.empty?
        %li{role: 'presentation', class: index == 0 ? 'active' : nil}
          %a{href: "##{tab_name(category)}", role: 'tab', data: {toggle: 'tab'}}
            = category.name.truncate(15, omission: '…')

  .col-sm-10
    .tab-content
      - @all_products.group_by(&:category).each do |category, products|
        .tab-pane.fade{id: tab_name(category), class: category == @categories.first ? 'in active' : nil}
          %h2.visible-print-block= category.name
          = render partial: 'products/modal_table', object: products, as: :products
