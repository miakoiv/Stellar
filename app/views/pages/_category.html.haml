%li.category{class: active_link_to_class(category.path)}
  = link_to category.path do
    .page-image
      = image_variant_tag category.resource.cover_image, navmenu ? :icon : :matchbox
    .page-title
      = category
