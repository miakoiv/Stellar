%li.has-image{class: is_active_link?(show_page_path(category_order)) && 'active mm-active'}
  - cache [navmenu, I18n.locale, :category_order_page, category_order] do
    = link_to show_page_path(category_order) do
      - if picture = category_order.cover_picture || category_order.resource.cover_picture
        .page-image
          = picture_variant_tag picture, :matchbox
      .page-title
        = category_order
