%li.has-image{class: primary.part_of_continuous_page? ? 'scroll' : is_active_link?(show_page_path(primary)) && 'active mm-active'}
  - cache [navmenu, I18n.locale, :primary_page, primary] do
    = link_to primary.part_of_continuous_page? ? show_page_path(primary.parent, trailing_slash: true, anchor: primary.slug) : show_page_path(primary) do
      - if primary.cover_picture.present?
        .page-image
          = picture_variant_tag primary.cover_picture, :matchbox
      .page-title
        = primary
