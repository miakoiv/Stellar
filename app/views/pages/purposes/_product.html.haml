%li.has-image{class: is_active_link?(show_page_path(product)) && 'active mm-active'}
  - cache [navmenu, I18n.locale, :product_page, product] do
    = link_to show_page_path(product) do
      - if product.resource.cover_picture.present?
        .page-image
          = picture_variant_tag product.resource.cover_picture, :matchbox
      .page-title
        = product
