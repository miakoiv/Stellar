.segment-content.promotion_feature{id: segment_content_id(promotion_feature, context), style: background_image_style(promotion_feature.cover_image)}
  - if promotion_feature.resource.present?
    = link_to show_promotion_path(promotion_feature.resource), class: 'foreground' do
      = image_variant_tag promotion_feature.foreground_image, :lightbox
