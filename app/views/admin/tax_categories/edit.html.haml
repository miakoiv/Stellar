.page-header
  = link_to admin_tax_categories_path, class: 'btn btn-default' do
    = icon('chevron-circle-left', t('actions.back'))
  = link_to admin_tax_category_path(@tax_category), class: 'btn btn-primary' do
    = icon('info-circle', t('actions.show'))
  %h1= icon('percent', @tax_category)

.row
  .col-md-6
    = render partial: 'form', locals: {locked: false}
