.page-header
  = link_to admin_pages_path, class: 'btn btn-default' do
    = icon('chevron-circle-left', t('actions.back'))
  %h1= icon('file-text-o', t('.new_page'))

.row
  .col-md-8
    = render partial: 'form', locals: {locked: false}
