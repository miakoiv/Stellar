.page-header
  = link_to admin_orders_path, class: 'btn btn-default' do
    = icon('chevron-circle-left', 'Back')
  - if current_user.can_update?(Order)
    = link_to edit_admin_order_path(@order), class: 'btn btn-primary' do
      = icon('pencil-square', 'Edit')
  %h1
    Order
    %span.small= @order.to_s

.row
  .col-sm-6
    .panel.panel-default
      .panel-body.form-horizontal
        .form-group
          %label.col-sm-4.control-label User
          .col-sm-8
            %p.form-control-static= @order.user
        .form-group
          %label.col-sm-4.control-label Order type
          .col-sm-8
            %p.form-control-static= @order.order_type
        .form-group
          %label.col-sm-4.control-label Ordered at
          .col-sm-8
            %p.form-control-static= @order.ordered_at
        .form-group
          %label.col-sm-4.control-label Approved at
          .col-sm-8
            %p.form-control-static= @order.approved_at

  .col-sm-6
    .table-responsive
      %table.table.table-striped.table-hover.table-loose
        %thead
          %tr
            %th.col-xs-3 Product code
            %th Product title
            %th.col-xs-2.text-right Amount

        %tbody
          = render partial: 'admin/order_items/order_item', collection: @order.order_items
