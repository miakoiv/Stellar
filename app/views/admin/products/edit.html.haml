.page-header
  = link_to admin_products_path, class: 'btn btn-default' do
    = icon('chevron-circle-left')
    Back
  = link_to admin_product_path(@product), class: 'btn btn-primary' do
    = icon('info-circle')
    Show
  %h1= icon('cube', @product)

.row
  .col-sm-6
    = render 'form'

  .col-sm-6
    = render partial: 'admin/images/dropzone', object: @product, as: :imageable

    #images.list-group.gallery
      = render partial: 'admin/images/edit_image', collection: @product.images, as: :image

:coffee
  $('#images').sortable(
    items: '.list-group-item'
    placeholder: '#{list_group_placeholder}'
  ).bind 'sortupdate', (e, ui) ->
    $.post '#{admin_reorder_images_path}',
      {reorder: $('.list-group-item', this).map(-> this.id).get()}
