.page-header
  = link_to admin_messages_path, class: 'btn btn-default' do
    = icon('chevron-circle-left', t('actions.back'))
  %h1
    = icon('envelope-o', t('.new_message'))

.row
  .col-md-6
    = render partial: 'form', locals: {locked: false}
