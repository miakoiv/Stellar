.page-header
  = link_to admin_inventories_path, class: 'btn btn-default' do
    = icon('chevron-circle-left', t('actions.back'))
  = link_to admin_inventory_path(@inventory), class: 'btn btn-primary' do
    = icon('info-circle', t('actions.show'))
  %h1= icon('cubes', @inventory)

.row
  .col-md-6
    = render partial: 'form', locals: {locked: false}
