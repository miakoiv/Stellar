.row
  .col-md-7
    .page-header
      %h1= icon('sitemap', title(Category, count: 2))
    #category-list.dd
      %ol.dd-list
        = render @categories
  .col-md-5
    .quick-edit-panel{data: {spy: 'affix'}}
      .header
        - if current_user.can_create?(Category)
          = link_to new_admin_category_path, remote: true, class: 'btn btn-primary' do
            = icon('plus', t('.new_category'))
      #category-form
        = render partial: 'form'

:coffee
  $(window).scrollTo $('##{dom_id(@category)}'), 300,
    offset: -$('.navbar').outerHeight true

  $('#category-list').nestable().on 'change', ->
    data = {rearrange: {categories: $(this).nestable('serialize')}}
    $.ajax
      type: 'POST'
      url: '#{rearrange_admin_categories_path}'
      data: JSON.stringify data
      contentType: 'application/json; charset=utf-8'
