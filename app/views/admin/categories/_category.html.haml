%li.dd-item{data: {id: category.id}, class: category.should_collapse? && !@category&.inside?(category) && 'dd-collapsed'}
  = render partial: 'item', object: category, as: :category
  - if category.children_count > 0
    %ol.dd-list
      = render category.children
