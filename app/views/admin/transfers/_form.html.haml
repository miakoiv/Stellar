= bootstrap_form_for [:admin, @transfer] do |f|
  .panel.panel-default
    .panel-body
      %fieldset{disabled: locked}
        .row
          .col-md-6
            = f.select :source_id, current_store.inventories.map { |i| [i.to_s, i.id] }, {include_blank: true}, {class: 'selectize'}
          .col-md-6
            = f.select :destination_id, current_store.inventories.map { |i| [i.to_s, i.id] }, {include_blank: true}, {class: 'selectize'}
        = f.text_field :note
      - unless locked
        %fieldset.actions.fixed
          .container
            = f.primary
