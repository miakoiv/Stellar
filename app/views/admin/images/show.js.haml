$("#{j render partial: 'admin/images/edit_row', object: @image, as: :image}").appendTo('#images');
$("##{dom_id(@image)}").initialize();
$("##{dom_id(@image)}").effect('highlight');

$('.sortable').sortable();
