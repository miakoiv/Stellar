= bootstrap_form_for [:admin, @shipping_method], layout: :horizontal,
    label_col: 'col-sm-4', control_col: 'col-sm-8' do |f|
  .panel.panel-default
    .panel-body
      %fieldset{disabled: locked}
        = f.text_field :name, required: true
        = f.select :shipping_gateway, Store.shipping_gateway_options, {}, {class: 'selectize'}
        = f.text_field :delivery_time, append: col(ShippingMethod, :delivery_time_unit), class: 'text-right'
        = f.text_field :enabled_at, data: {provide: 'datepicker', 'date-format' => 'yyyy-mm-dd', 'date-language' => I18n.locale}
        = f.text_field :disabled_at, data: {provide: 'datepicker', 'date-format' => 'yyyy-mm-dd', 'date-language' => I18n.locale}
        = f.select :shipping_cost_product_id, current_store.products.virtual.map { |p| [p.to_s, p.id] }, {include_blank: blank}, {class: 'selectize'}
        = f.select :detail_page_id, current_store.pages.secondary.map { |p| [p.to_s, p.id] }, {include_blank: blank, help: t('.detail_page_help')}, {class: 'selectize'}
        = f.text_area :description
      - unless locked
        %fieldset.actions
          = f.form_group do
            = f.primary

:coffee
  $('#shipping_method_description').summernote
    lang: "#{t('culture')}"
    toolbar: [
      ['para', ['style', 'ul', 'ol', 'paragraph']]
      ['font', ['bold', 'italic', 'clear']]
    ]
    dialogsFade: true
