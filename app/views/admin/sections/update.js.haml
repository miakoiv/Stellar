$('#editor-panel .section-form').replaceWith("#{j render partial: 'admin/sections/form', object: @section, as: :section}");
$("##{dom_id(@section)} > .section").replaceWith("#{j render @section}");
$('##{dom_id(@section)}').initialize();
viewportUpdate();
