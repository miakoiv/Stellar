.section-content{class: [section.layout, section.width, section.viewport? && 'viewport', section.swiper? && 'swiper', section.reverse? && 'reverse']}
  .row
    = render partial: 'admin/columns/column', collection: section.columns
