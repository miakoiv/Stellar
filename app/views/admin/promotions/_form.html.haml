= bootstrap_form_for [:admin, @promotion], layout: :horizontal,
    label_col: 'col-sm-4', control_col: 'col-sm-8' do |f|
  .panel.panel-default
    %fieldset.panel-body{disabled: locked}
      = f.text_field :name
      = f.select :promotion_handler_id,
          PromotionHandler.all.map { |h| [h.to_s, h.id] },
          {}, {class: 'selectize', disabled: !@promotion.new_record?}
      = f.date_field :first_date,
        data: {provide: 'datepicker', 'date-format' => 'yyyy-mm-dd', 'date-language' => I18n.locale}
      = f.date_field :last_date,
        data: {provide: 'datepicker', 'date-format' => 'yyyy-mm-dd', 'date-language' => I18n.locale}
      - unless locked
        = f.form_group do
          = f.primary
