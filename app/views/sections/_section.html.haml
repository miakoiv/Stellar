%section.section{style: section_style(section)}
  - if section.spread?
    .section-content{class: [section.outline, section.layout, section.width]}
      .row
        = render partial: 'segments/segment', collection: section.segments
  - else
    .container
      .row
        .section-content{class: [section.outline, section.layout, section.width]}
          .row
            = render partial: 'segments/segment', collection: section.segments
